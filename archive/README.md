# Local Archive

This directory holds workstation-only material removed from the active project
tree but retained in case it is useful later. Dated archive contents are ignored
by Git; only this policy file is tracked.

Generated caches and other safely reproducible files should be deleted instead
of archived. Credentials, tokens, live databases, and active application state
must not be placed here.
